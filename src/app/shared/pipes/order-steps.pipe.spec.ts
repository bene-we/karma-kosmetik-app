import { OrderStepsPipe } from './order-steps.pipe';

describe('OrderStepsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderStepsPipe();
    expect(pipe).toBeTruthy();
  });
});
